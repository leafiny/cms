{% set data = page.getCustom('page') %}

<div class="cms-page">
    <h2>{{ data.getTitle }}</h2>

    {% set mainImage = page.getMainImage(data.getPageId) %}
    {% if mainImage %}
        <div class="page-image">
            <img src="{{ page.getMediaUrl }}{{ page.imageResize(mainImage.getImage, 940, 940) }}" class="pure-u-1" alt="{{ category.getName }}" />
        </div>
    {% endif %}

    <div class="page-view">
        {{ include(template_from_string(data.getContent)) }}
    </div>
</div>